{
  "name": "noter",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "run:api": "cd api; npm run start",
    "dev:api": "cd api; npm run dev",
    "dev:react": "cd react-app; npm run start",
    "dev": "concurrently \"npm:dev:api\" \"npm:dev:react\" --kill-others"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "concurrently": "^4.0.1",
    "dotenv": "^6.0.0",
    "keyword-extractor": "0.0.15",
    "sentence-extractor": "^1.0.7"
  }
}
